{
  "name": "bulk-functions",
  "description": "Backend de Cloud Functions para el m\u00f3dulo Bulk (usuarios, notificaciones, IA)",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test test/"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.0",
    "googleapis": "^144.0.0"
  },
  "optionalDependencies": {
    "twilio": "^5.3.0"
  }
}